{
  "name": "revamp-studio",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.11.1",
  "scripts": {
    "dev": "pnpm -r --filter @revamp/core-types --filter @revamp/design-system --filter @revamp/editor-engine --filter @revamp/recording-engine --filter @revamp/render-engine build && pnpm --filter @revamp/desktop dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.8.2"
  }
}
